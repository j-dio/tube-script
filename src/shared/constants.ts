export const EXTENSION_NAME = 'TubeScript'

/** Built offscreen document path (see `vite.config.ts` input + dist layout). */
export const OFFSCREEN_DOCUMENT_PATH = 'offscreen.html'

/** Static MAIN-world scripts under `public/inject/` (CSP-safe on strict pages like YouTube). */
export const READ_YT_PLAYER_INJECT_FILE = 'inject/read-yt-initial-player.js'
export const READ_WATCH_TITLE_INJECT_FILE = 'inject/read-watch-title.js'
