/**
 * Parse `ytInitialPlayerResponse` / page context and build caption track metadata.
 * Implemented in a later phase (PRD §2.3).
 */
export function extractPageCaptionContext(): null {
  return null
}
