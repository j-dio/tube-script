import type { CaptionSegment } from '@/shared/types'

/** Stage 1: PARSE — caption XML/JSON → segments (PRD §5.1). */
export function parseCaptionPayload(_raw: string): CaptionSegment[] {
  return []
}
